<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Phases 1-3 were fully offline by design. Phase 4 adds real network calls (to this app's
      OWN backend only — see backend/README.md — never directly to Gemini or a Google API), so
      INTERNET is now required.
      Deliberately NOT added: CAMERA (Phase 4's "photographed document" sources are supplied via
      the existing Storage Access Framework file picker, which needs no runtime permission — a
      dedicated in-app camera capture (CameraX) was judged a larger, separate feature than this
      phase's actual ask and is left as a future enhancement) and any storage permission (SAF
      needs none either).
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".InventorySmartApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventorySmartAI"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.InventorySmartAI"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
